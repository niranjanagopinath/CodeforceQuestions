number=int(input())
home=[]
guest=[]
count=0
for i in range(number):
    h,g=map(int,input().split())
    home.append(h)
    guest.append(g)
for i in home:
    for j in guest:
        if i==j:
            count+=1
print(count)
